name: Guard Debug Flags

on:
  push: {}
  pull_request: {}
  workflow_dispatch: {}

jobs:
  debug_flags:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Guard debug flags
        run: node scripts/ensureDebugFlagsDisabled.mjs
